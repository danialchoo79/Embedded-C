#include <stdio.h>

int main(void)
{
	int height = 0;
	int row = 1;

	printf("Enter height of pyramid: ");
	fflush(stdout);

	scanf("%d",&height);

	for(int i=0; i <= height; i++)
	{
		for(int j=0; j<=row; j++)
		{
			printf("%d",row);
			fflush(stdout);
		}
		printf("\n");
		row++;
	}
	return 0;
}
